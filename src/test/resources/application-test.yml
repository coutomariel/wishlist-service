server:
  port: 8080

spring:
  data:
    mongodb:
      uri: ${URI:mongodb://admin:admin@localhost:27017/}
      database: ${DATABASE:wishlistdb}

wishlist:
  limit-products: ${WISHLIST_LIMIT_PRODUCTS:20}
springdoc:
  api-docs:
    path: /api-docs
